# Privacy

This plugin sends tool inputs and configured Oracle connection credentials only to the Oracle database endpoint selected by the Dify workspace user. It does not collect telemetry or send data to any additional service.

Query text, prompts, generated SQL, and result rows may contain sensitive data. Review workflow access and database grants before using the plugin in production.
